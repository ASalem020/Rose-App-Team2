'use client';


// Imports


import ProfileForm from '@/components/features/account/profile-form';


// Page


/**
 * Profile page (/profile) — thin wrapper around the shared ProfileForm
 */
export default function Profile() {
  return <ProfileForm />;
}
